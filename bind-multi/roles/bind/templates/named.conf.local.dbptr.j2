$TTL    604800
@       IN      SOA    {{primary}}. admin.{{primary}}. (
                              3         ; Serial
                         604800         ; Refresh
                          86400         ; Retry
                        2419200         ; Expire
                         604800 )       ; Negative Cache TTL
; name servers
      IN      NS      ns1.{{ primary }}.
      IN      NS      ns2.{{ primary }}.

; PTR Records
{% for x in ptr_records %}
{{x.ptr}}  IN    PTR {{x.host}}.{{ primary}}.    
{% endfor %}
